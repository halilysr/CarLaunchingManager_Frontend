import { PhotofilterbycarPipe } from './photofilterbycar.pipe';

describe('PhotofilterbycarPipe', () => {
  it('create an instance', () => {
    const pipe = new PhotofilterbycarPipe();
    expect(pipe).toBeTruthy();
  });
});
